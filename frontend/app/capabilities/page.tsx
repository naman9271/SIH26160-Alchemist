import { AppShell } from "../components/app-shell";

const sections: Array<[string, string[]]> = [["Available now", ["Classic-PCAP upload and passive analysis", "IKE / ESP / AH / NAT-T observation", "Metadata-only flow features and optional ML", "Fusion provenance, deterministic security score, risk and PDF report"]], ["Authorised lab mode", ["Local tcpdump capture with BPF filters", "strongSwan VICI gateway facts", "Linux XFRM state, policy, and replay telemetry", "Deep assessment availability checks"]], ["Deliberate boundaries", ["No ESP payload decryption", "UNKNOWN is never silently converted into a positive result", "No browser-to-gRPC or browser-to-ML-worker connection", "No automated VPN provisioning in the deployed analyzer"]]];
export default function CapabilitiesPage() { return <AppShell><main className="view-main"><header className="page-heading"><p>Capability map</p><h1>Know the boundary of the evidence.</h1><span>The analyzer is strongest when it says what it knows, why it knows it, and what remains out of reach.</span></header><div className="capability-list">{sections.map(([label, values]) => <section className="surface-panel" key={label}><div className="panel-kicker">{label}</div><ul>{values.map(value => <li key={value}><span>↗</span>{value}</li>)}</ul></section>)}</div></main></AppShell>; }
